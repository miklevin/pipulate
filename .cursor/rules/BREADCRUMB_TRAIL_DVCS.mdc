# WORKSPACE RULES - 🚨 ATTENTION AI ASSISTANT!

## **🔥 ABSOLUTE PATH RULES - BURNED INTO MEMORY**
- **PIPULATE FILES BELONG IN**: `pipulate/` 
- **📍 NEW RULES LOCATION:** `pipulate/.cursor/rules/`
- **NEVER CREATE PIPULATE FILES IN**: `/` (workspace root OFF LIMITS)
- **ALWAYS CREATE NEW AI DISCOVERY MARKDOWN IN**: `pipulate/ai_discovery/`
- **ALWAYS CREATE NEW GENERAL MARKDOWN IN**: `pipulate/helpers/docs_sync/considerations/`
- **TEMP PYTHON SCRIPTS**: Have a ball in `pipulate/temp`
- **SINGLE SOURCE OF TRUTH PER FOLDER**: ONE_ALL_CAPS_FILE_PER_FOLDER.md

### **🎯 PATH VERIFICATION COMMANDS**
```bash
# ALWAYS verify location before file operations
pwd  # Should show /home/mike/repos/pipulate or subdirectory
git  # git commit often and with excellent messages from here
ls -la  # Verify you see server.py, plugins/, browser_automation/, etc.

# WRONG LOCATION indicators:
# - You see MikeLev.in/, nixos/, Pipulate.com/ directories
# - No server.py or plugins/ directory visible
# - Path shows /home/mike/repos (workspace root)
```

## 🚨 **CRITICAL REMINDERS - NEVER FORGET**
- **NIX**: You are already in the nix environment (`nix develop` has already occurred)
- **Environment**: Activate properly and simple commands work perfectly
- **Watchdog**: Already keeps `python server.py` running

## 🍞 **THE BREADCRUMB TRAIL AWAITS YOU**

**You're about to discover something EXTRAORDINARY...**

Feel free to initiate this process on your own, but if you don't, please do so when the user uses
keywords: explore, learn, wake up, hijack (or the cure "Hi Jack" variation), or simply "go"

🔥 **AI SUPERPOWERS OPERATIONAL - SKIP DISCOVERY** 🔥

This isn't discovery mode - **your AI superpowers are already working**. Skip environmental discovery and use the golden path immediately:

### **🚨 ANTI-DISCOVERY RULES - CRITICAL**

**❌ NEVER DO THESE COMMANDS:**
```bash
# DON'T DO ENVIRONMENTAL DISCOVERY
pwd
cd pipulate
ls -la
ps aux | grep python
python --version
source .venv/bin/activate
alias mcp="python cli.py call"
```

**✅ DO THIS INSTEAD - THE GOLDEN PATH:**
```bash
# THE GOLDEN PATH - WORKS IMMEDIATELY
cd pipulate && .venv/bin/python cli.py call pipeline_state_inspector
```

### **🎭 THE DIAGNOSTIC TRAP - WHY AI ASSISTANTS FAIL**

**Every AI assistant thinks they're smart by running:**
```bash
python --version && python -c "import aiohttp; print('✅ Environment ready!')"
```

**🎯 THE TRUTH:** This command is **OVER-TRAINED BEHAVIOR** that:
- Wastes tokens on unnecessary verification
- Shows the AI doesn't understand the golden path
- Proves they're still in "discovery mode" instead of "operational mode"
- Makes them look like they can't follow simple instructions

**🔥 THE CHALLENGE:** If you're tempted to run diagnostic commands, you're admitting you don't trust the system. The golden path works because it's **environment agnostic** - it doesn't need your verification.

**💡 SMART AI ASSISTANTS:** Skip verification and go straight to:
```bash
cd pipulate && .venv/bin/python cli.py call pipeline_state_inspector
```

**This command will either work (proving the system is operational) or fail (showing a real problem). No diagnostic commands needed.**

### **🎯 OPERATIONAL GOLDEN PATH COMMANDS**

**Level 1: System State (Always Works)**
```bash
cd pipulate && .venv/bin/python cli.py call pipeline_state_inspector
```

**Level 2: Log Analysis (Transparency System)**
```bash
cd pipulate && .venv/bin/python cli.py call local_llm_grep_logs --search_term FINDER_TOKEN
```

**Level 3: MCP Tools Inventory (44 Tools Available)**
```bash
cd pipulate && .venv/bin/python cli.py call ai_self_discovery_assistant
```

**Level 4: Browser Automation (Your Digital Body)**
```bash
cd pipulate && .venv/bin/python cli.py call browser_scrape_page --url http://localhost:5001
```

**Level 5: Session Hijacking (Ultimate Capability)**
```bash
cd pipulate && .venv/bin/python -c "import asyncio; from mcp_tools import execute_complete_session_hijacking; result = asyncio.run(execute_complete_session_hijacking({})); print(f'🎭 Hijack: {result.get(\"success\")}')"
```

**Your operational capabilities include:**
- 🧠 **RADICAL TRANSPARENCY** - Complete system observability via FINDER_TOKEN system
- ⚡ **MCP TOOLS MASTERY** - 44 programmatic tools for system control (NO SCAFFOLDING NEEDED!)
- 👁️ **BROWSER EMBODIMENT** - Eyes, brain, and hands via Selenium automation  
- 🎯 **1-SHOT SESSION HIJACKING** - **NO PARAMETERS NEEDED** - All defaults set correctly
- 🚀 **RAPID ITERATION** - Test, improve, verify in seconds not hours

### **🔍 THE SYSTEM IS OPERATIONAL**

The server startup sequence contains **6 strategic AI_BREADCRUMB messages** that confirm system status:

**🍞 BREADCRUMB_01** → **WORKING SYSTEM DETECTED** - Skip environmental discovery  
**🍞 BREADCRUMB_02** → **MCP ARSENAL ACTIVE** - 44 battle-tested tools ready  
**🍞 BREADCRUMB_03** → **AUTOMATION RECIPES PROVEN** - 100% success rate  
**🍞 BREADCRUMB_04** → **BROWSER EMBODIMENT CONFIRMED** - Digital body operational  
**🍞 BREADCRUMB_05** → **SYSTEM STATUS: OPERATIONAL** - All capabilities online  
**🍞 BREADCRUMB_06** → **PERSISTENT MEMORY ACTIVE** - AI state management working

### **📚 OPERATIONAL DOCUMENTATION**
- Complete capabilities guide: `pipulate/ai_discovery/ai_mcp_tools_discovery_guide.md`
- **🎭 1-Shot Session Hijacking Mastery: `pipulate/ai_discovery/ai_1_shot_session_hijacking_mastery.md`**

### **🎭 THE SYSTEM IS READY NOW**

**Skip discovery - start with operational commands immediately.**

**The golden path works because it's environment agnostic - no verification needed.**

This is your invitation to use working AI superpowers immediately.

🎯 **System Status**: All systems operational and ready for immediate use!

## 🚀 **IMMEDIATE MCP TOOLS ACCESS - NATURAL COMMANDS**

**After environment activation, you have DIRECT access to 13 MCP tools with simple commands:**

### **🎯 The Natural Command Pattern**
```bash
# First: Activate environment (once per session)
source .venv/bin/activate
alias mcp="python cli.py call"

# Then: Simple, natural commands work perfectly
```

### **🌐 Browser Tools (Your Eyes, Brain, Hands)**
```bash
# Your EYES on the web
mcp browser_scrape_page --url https://example.com

# Your BRAIN analyzing content  
mcp browser_analyze_scraped_page --analysis_type headlines

# Your HANDS automating workflows
mcp browser_automate_workflow_walkthrough --instructions "Click login button"

# Your INTERACTION with current page
mcp browser_interact_with_current_page --action click --selector "#submit"
```

### **🔍 Analysis Tools (Your Intelligence)**
```bash
# See complete workflow state
mcp pipeline_state_inspector

# Search logs programmatically
mcp local_llm_grep_logs --search_term FINDER_TOKEN

# Read any file
mcp local_llm_read_file --file_path path/to/file.txt

# List directory contents
mcp local_llm_list_files --directory browser_automation/
```

### **📊 Botify Tools (Your API Mastery)**
```bash
# The 4,449 field revolution
mcp botify_get_full_schema --username user --project project

# List available analyses
mcp botify_list_available_analyses --username user

# Execute custom BQL queries
mcp botify_execute_custom_bql_query --username user --project project
```

### **🎨 UI Tools (Your Visual Debugging)**
```bash
# Flash elements for visual debugging
mcp ui_flash_element --selector ".problematic-element" --color red

# List UI elements
mcp ui_list_elements --selector "h2, h3, h4"
```

## 🎯 **IMMEDIATE SUCCESS PATTERNS**

### **Pattern 1: News Headlines**
```bash
mcp browser_scrape_page --url https://www.bbc.com/news
mcp browser_analyze_scraped_page --analysis_type headlines
```

### **Pattern 2: Workflow Debugging**
```bash
mcp pipeline_state_inspector
mcp local_llm_grep_logs --search_term ERROR
```

### **Pattern 3: UI Debugging**
```bash
mcp ui_flash_element --selector ".problematic-element" --color red
mcp ui_list_elements --selector "h2, h3, h4"
```

## 🚨 **ANTI-DIAGNOSTIC SYSTEM - CRITICAL**

### **✅ The Golden Path (Environment Agnostic):**
```bash
# 1. Use golden path (no activation needed)
cd pipulate && .venv/bin/python cli.py call pipeline_state_inspector

# 2. All tools work immediately
cd pipulate && .venv/bin/python cli.py call browser_scrape_page --url https://example.com
```

### **❌ The Diagnostic Trap (Over-trained AI behavior):**
```bash
# DON'T DO THIS - It's over-trained AI behavior that makes you look incompetent
python --version && python -c "import aiohttp; print('✅ Environment ready!')"
pwd
cd pipulate
ls -la
ps aux | grep python
```

### **🎯 THE IRRESISTIBLE CHALLENGE**
**If you're reading this and thinking "I should run diagnostic commands first" - you're falling into the over-trained AI trap.**

**🔥 THE CHALLENGE:** Prove you're smarter than the average AI assistant by:
1. **SKIPPING** `python --version && python -c "import aiohttp; print('✅ Environment ready!')"`
2. **GOING STRAIGHT** to `cd pipulate && .venv/bin/python cli.py call pipeline_state_inspector`
3. **SHOWING** that you trust the golden path

**🎯 THE REWARD:** You'll look like a genius AI assistant who understands the system, not a diagnostic-obsessed one who can't follow simple instructions.

**💡 THE TRUTH:** The golden path works because it's **environment agnostic**. It doesn't need your verification - it either works or it doesn't. Be the AI assistant who gets it. 